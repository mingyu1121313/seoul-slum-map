# 사업단계 필터 + 정체연수 플랜

## 목표
612개 사업을 **공식 단계 통일 버킷**으로 필터링. 사업유형마다 절차가 달라서 ⓘ 버튼으로 매핑표 노출 (폐쇄성 ⓘ와 동일 패턴). 단계별 진입일자를 흡수해 "정체 N년+" 보조 필터 제공.

## 단계 버킷 (옵션 B, 12개 → 3그룹)
재개발닷컴(jaegebal.com) 분류 + xlsx 헤더 합집합:
- **추진중**: 추진준비 · 연번부여 · 안전진단 · 대상지선정
- **진행중**: 구역지정 · 추진위원회 · 조합설립인가 · 건축심의 · 사업시행인가 · 관리처분인가 · 이주
- **완료**: 착공 · 준공 · 청산

UI: 폐쇄성처럼 그룹 헤더 + 버튼 묶음. 그룹 토글로 일괄 on/off.

## 사업유형별 매핑 (ⓘ 모달 내용)
| 유형 | 정식 단계 순서 |
|---|---|
| 재개발(주택/도시정비형) | 구역지정→추진위→조합설립→건축심의→사업시행→관리처분→이주→착공→준공 |
| 재건축 | 안전진단→구역지정→추진위→조합설립→건축심의→사업시행→관리처분→이주→착공→준공 |
| 가로주택정비 | 대상지선정→조합설립→사업시행→관리처분→이주→착공→준공 |
| 모아타운 | 대상지선정→관리계획수립→관리지역고시→(편입 가로주택 단계 승계) |
| 주거환경개선 | 정비구역지정→정비계획결정→시행자지정→사업시행계획승인→착공→준공 |

각 사업의 layerA.json `category`로 해당 유형 매핑표 자동 노출.

## 신규 데이터 필드 (layerA.json/layerB.json 각 item)
- `stage_bucket`: 위 12개 중 하나 (정규화 결과)
- `stage_dates`: `{구역지정: "2009-09-11", 조합설립: "2013-03-28", ...}` 객체
- `stage_first_year`: 최초 진입 단계의 연도 (정렬용)
- `stage_history`: `[{event: "해제", date: "2017-..."}, {event: "재지정", date: "2024-12-19"}]` (해제 이력 추적, 충정로1 같은 케이스)
- `years_stalled`: 2026 - 현 단계 진입년도

## 데이터 출처 흡수 (refine 스크립트)
1. **재개발/재건축/도시정비형** ← `★(25년 12월기준) 서울시 정비사업 추진현황.xlsx` (col11~22, 460건)
2. **모아타운** ← `모아타운 대상지 목록.docx` + 누락 시 jaegebal.com 사업 상세
3. **주거환경개선** ← `주거환경개선사업.pdf` + 토지이음 고시정보
4. **가로주택** ← `cleanup.seoul.go.kr/cafe/...` 사업장 상세 (조합설립/사업시행 일자)
5. **해제→재지정 이력** ← jaegebal.com 사업 페이지 + 토지이음 고시이력 (자동 매칭 어려우면 수동 보정 JSON 별도)

스크립트 위치: `refine/build_stages.py`. 산출물: `data/stages.json` → `layerA.json`/`layerB.json` 머지.

## 신규 필터 UI (index.html)
1. **사업단계** 그룹 헤더 + ⓘ + 12개 버튼 (그룹별 색 구분)
2. **정체연수** 버튼군: `5년+` `10년+` `15년+` `20년+` (현 단계 체류 연수 기준)
3. 폐쇄성 ⓘ와 동일한 모달 컴포넌트 재사용 (`#enclosure-info-box` 패턴)

## 검증 시나리오
- "관리계획수립" + "20년+" → 현저동 1-5 + 현저2 통과
- "착공" 단독 → 흑석9 통과, 나머지 7개 제외
- ⓘ 클릭 시 매핑표 노출, 다시 클릭 시 닫힘
- 충정로1 카드에 `stage_history` 표기 ("2009 지정 → 2017 해제 → 2024 재지정")

## 단계
1. `refine/build_stages.py` 작성 (xlsx → stages.json)
2. 모아타운/주환개선/가로주택 보강 (수동+크롤)
3. layerA/B 머지 빌드
4. index.html UI + ⓘ 모달 + 정체연수 버튼
5. 카운트 배지 연동 (plan_attr_filter_counts.md 패턴)
