# 폴리곤 정확도 수정 플랜

## 문제
의제처리구역 SHP에 1970-80년대 **토지구획정리사업·택지개발** 광역 legacy zone이
포함되어 있어, 사이트가 거대 테두리(700~1300 ha)에 매칭됨.

| 카테고리 | 거대 zone(30ha+) 매칭 비율 |
|---|---|
| 지역주택 | **80%** (16/20) |
| 형성유형(layerB) | **46%** (6/13) |
| 가로주택정비 | 29% (36/126) |
| 재개발(주택정비형) | 15% (28/184) |
| 주거환경개선(정비형) | 7% (1/14) |

대표 원인 zone: 영동1·잠실·가락·경인·망우·개포·시흥·영등포 토지구획정리사업
→ 이들은 `MLSFC_CL/SCLAS_CL`이 모두 `None`이고 `DGM_NM`에
`토지구획정리사업` 또는 `택지개발` 문자열 포함.

## 수정 방안
### 1. `scripts/build_polygons.py`에 zone 필터 추가
공간조인 **전에** 다음 zone들을 zones GDF에서 제외:
- `MLSFC_CL is None` 또는
- `DGM_NM` contains (`토지구획정리`, `택지개발`, `토지구획`)
- 안전망: `DGM_AR > 500_000` (50 ha 이상) zone도 제외

### 2. 재실행
```bash
python3 scripts/build_polygons.py    # polygons.json 재생성
python3 scripts/build_bldg_age.py    # 노후도 재계산
```

### 3. 결과 검증
- 매칭률은 357 → 약 280~300건으로 감소 예상
- **거대 zone 매칭 0건**이 되어야 함 (검증 코드 추가)
- 매칭 잃은 사이트는 자동으로 **포인트 마커로 fallback** (createMarker가 polygons.json
  체크 후 polygon 없으면 마커 생성하므로 코드 변경 불필요)

### 4. 커밋 + 푸시
지도 표시 검증 후 commit & push → slum-seoul.xyz 자동 배포.

## 예상 결과
- 형성유형 매칭 13 → 7건 (대부분 마커로 복귀, 정확)
- 지역주택 매칭 20 → 4건 (대부분 마커로 복귀, 정확)
- 거대 테두리 0건
