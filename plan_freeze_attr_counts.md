# 속성 필터 카운트/리스트 고정 + 통과 목록 위치 이동

## 목표
- **위치 이동**: "필터 통과 목록" 섹션을 "속성 필터" 헤더 바로 아래 (경사지 위)로 이동
- **카운트 고정**: 속성 필터 내부 개별 숫자(경사지 190, 직주근접 79, 10년+/20년+/30년+/40년+, 상/중/하 등) 및 아코디언 펼친 리스트는 전체 612 기준 고정값으로 표시
- **변동 유지**: "속성 필터" 헤더 옆 큰 숫자(612)와 "필터 통과 목록" 카운트/리스트만 현재 필터 조합에 따라 변동

## 동작 방식
- 개별 체크박스/버튼(경사지, 직주근접, 10년+/20년+ 버튼, 상/중/하)은 그대로 작동
- 이들은 지도 필터링 + 상단 612 숫자 + 필터 통과 목록에만 반영
- 각 속성 카운트 배지(`attr-count-*`, `btn-age-*-cnt`, 폐쇄성 등급 카운트)는 전체 기준으로 고정
- 아코디언 펼친 리스트(경사지 190건, 직주근접 GBD/CBD/YBD, 노후도 연차순, 폐쇄성 등급별)도 전체 기준 고정

## 수정 지점 (index.html)
1. **HTML 구조** — `속성 필터` 섹션 내부에서 `필터 통과 목록` 블록을 `경사지` 필터 행 위로 이동
2. **`updateFilterCounts()`** — 개별 카운트 배지(경사지 190, 직주근접 79, 노후도 10~40년+ 버튼, 폐쇄성 상/중/하)를 현재 필터 영향 받지 않도록 전체 612 기준으로만 계산
3. **`buildAttrFilterBody()`** — `slopeItems`/`nearItems`/`ageItems`/`encItems` 수집 시 다른 필터 상태(`passesFiltersWith`) 무시하고 해당 속성 조건만으로 포함. (단, 노후도·폐쇄성 리스트는 버튼 선택이 없으면 비어있으므로 버튼 선택 시만 해당 조건의 전체 목록)
4. **필터 통과 목록** — 기존 `buildPassedList()` 로직 유지 (현재 필터 조합 기준)

## 검증
- 경사지 체크 → 지도는 경사지만, 속성 필터 내부 숫자/리스트는 그대로 190/79/등, 상단 612 숫자는 190으로, 통과 목록에 190건만
- 경사지 + 노후도 30년+ 체크 → 속성 내부 숫자/리스트 불변, 상단 감소, 통과 목록에 교집합만
